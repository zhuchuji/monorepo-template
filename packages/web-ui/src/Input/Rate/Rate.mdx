---
name: Rate
menu: web-ui
---

import { Playground, Props } from 'docz';
import { useState } from 'react';
import { Rate } from '../../';

### Basic usage
<Playground>
  {() => {
    const [rate, setRate] = useState();
    return <Rate value={rate} onChange={rate => setRate(rate)} />
  }}
</Playground>

## Props
<Props of={Rate} />
