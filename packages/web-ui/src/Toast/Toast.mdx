---
name: Toast
menu: web-ui
---

import { Playground, Props } from 'docz';
import { toast } from '../../';

# Toast

### Basic usage

<Playground>
  <button onClick={() => toast('a toast')}>show toast</button>
  <button onClick={() => toast('info toast', { type: 'info' })}>info toast</button>
  <button onClick={() => toast('success toast', { type: 'success' })}>success toast</button>
  <button onClick={() => toast('error toast', { type: 'error' })}>error toast</button>
</Playground>

## Props
<Props of={toast} />
