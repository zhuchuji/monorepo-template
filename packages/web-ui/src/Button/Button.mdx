---
name: Button
menu: web-ui
---

import { Playground, Props } from 'docz';
import styled from 'styled-components';
import { Button } from '../';

### type

By default type="contained"

<Playground>
  <Button type="contained">contained</Button>
  <Button type="outlined">outlined</Button>
  <Button type="text">text</Button>
</Playground>

### color

<Playground>
  <Button color="default">default</Button>
  <Button color="primary">primary</Button>
  <Button color="secondary">secondary</Button>
  <Button color="inherit">inherit</Button>
</Playground>

### size

<Playground>
  <Button size="small">small</Button>
  <Button size="medium">medium</Button>
  <Button size="large">large</Button>
</Playground>

### round

<Playground>
  <Button round>round</Button>
  <Button round size="medium">round medium</Button>
</Playground>

### disabled

<Playground>
  <Button disabled>disabled</Button>
  <Button type="outlined" disabled>outline disabed</Button>
  <Button type="text" disabled>text disabled</Button>
</Playground>

### full width

<Playground>
  <Button fullWidth>fullWidth</Button>
</Playground>

### href link

<Playground>
  <Button href="/">href</Button>
</Playground>

### loading

<Playground>
  <Button loading>loading</Button>
  <Button loading size="medium">loading medium</Button>
  <Button loading size="large">loading large</Button>
  <Button loading type="outlined">loading outline</Button>
</Playground>

### type + color

<Playground>
  <Button type="outlined" color="primary">outlined + primary</Button>
  <Button type="outlined" color="inherit">outlined + inherit</Button>
</Playground>

### custom

<Playground>
  {() => {
    const ActionButton = styled(Button)`
      color: red;
    `;
    return (
      <ActionButton>custom button</ActionButton>
    );
  }}
</Playground>
