---
name: Transition
menu: web-ui
---

### FadeIn

```tsx
import { useState } from 'react'
import { FadeIn } from '.'

const [show, toggleShow] = useState(false);
;
<>
<button onClick={() => toggleShow(show => !show)}>toggle</button>
{show ? <FadeIn>fade in content</FadeIn> : null}
</>
```
